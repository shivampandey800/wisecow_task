apiVersion: v1
kind: Service
metadata:
  name: wisecow-service
spec:
  selector:
    app: wisecow
  ports:
    - protocol: TCP
      port: 80       # Service port exposed
      targetPort: 4499  # Pod port where wisecow listens
  type: ClusterIP
